{
    "version": "2.0.0",
    "tasks": [
        {
            "label": "Build All",
            "type": "shell",
            "command": "powershell",
            "args": ["-ExecutionPolicy", "Bypass", "-File", "build-all.ps1"],
            "options": {
                "cwd": "${workspaceFolder}/build-scripts"
            },
            "problemMatcher": []
        }
    ]
}
